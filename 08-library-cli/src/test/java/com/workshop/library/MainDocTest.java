package com.workshop.library;

import org.junit.jupiter.api.Test;
import static org.junit.jupiter.api.Assertions.*;

// Intentionally minimal; refactor to extract domain and test it separately.
public class MainDocTest {
    @Test void placeholder(){ assertTrue(true); }
}
